<controls:MetroWindow 
    xmlns:controls="clr-namespace:SixCloudCustomControlLibrary.Controls;assembly=SixCloudCustomControlLibrary"
    x:Class="SixCloud.Views.OfflineUrlsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:local="clr-namespace:SixCloud.Views"
    mc:Ignorable="d"
    BorderBrush="{DynamicResource PrimaryHueDarkBrush}"
    Title="{Binding Title}" Height="Auto" Width="Auto" MetroWindowLayout="WideAngle" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Topmost="True">
    <!--<Window.Resources>
        <Style x:Key="TabItemStyle" TargetType="{x:Type TabItem}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="6,2,6,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <StackPanel x:Name="templateRoot" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" 
                                              ContentSource="Header" 
                                              Focusable="False" 
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                            <Rectangle x:Name="line" Fill="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Bottom" Height="3" Margin="2"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Foreground" Value="Gray"/>
                                <Setter Property="Opacity" Value="0.5"/>
                            </MultiDataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <TabControl x:Name="MainTab" Margin="60 40 60 30" WindowChrome.IsHitTestVisibleInChrome="True" Background="{x:Null}" BorderBrush="{x:Null}" ItemContainerStyle="{DynamicResource TabItemStyle}">
        <TabItem
                 IsSelected="True"
                 Header="链接"
                 Background="{x:Null}"
                 BorderBrush="{x:Null}">
            <Grid>
                <Grid x:Name="Link" OpacityMask="White">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBox Margin="15"
                 FontSize="16"
                 BorderBrush="{DynamicResource PrimaryHueMidBrush}"
                 materialDesign:HintAssist.Hint="输入离线下载的任务链接，每行一个"
                 Foreground="{DynamicResource PrimaryHueDarkBrush}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right">
                        <Button Margin="15" Padding="45,0" Content="确定"/>
                        <Button Margin="15" Padding="45,0" Content="取消"/>
                    </StackPanel>

                </Grid>
                <Grid.RenderTransform>
                    <TranslateTransform x:Name="LinkTranslateTransform"/>
                </Grid.RenderTransform>
            </Grid>
            <TabItem.Triggers>
                <EventTrigger RoutedEvent="Selector.Selected">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation BeginTime="00:00:00"
                                             Duration="00:00:00.500"
                                             Storyboard.TargetName="Link"
                                             Storyboard.TargetProperty="OpacityMask.Opacity"
                                             From="0"
                                             To="1">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase EasingMode="EaseIn"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation BeginTime="00:00:00"
                                             Duration="00:00:00.250"
                                             Storyboard.TargetName="LinkTranslateTransform"
                                             Storyboard.TargetProperty="X"
                                             From="-100"
                                             To="0"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </TabItem.Triggers>
        </TabItem>
        <TabItem Header="种子文件"
                 Background="{x:Null}"
                 BorderBrush="{x:Null}">
            <Grid>
                <Grid x:Name="Seed" OpacityMask="White">

                </Grid>
                <Grid.RenderTransform>
                    <TranslateTransform x:Name="SeedTranslateTransform"/>
                </Grid.RenderTransform>
            </Grid>
            <TabItem.Triggers>
                <EventTrigger RoutedEvent="Selector.Selected">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation BeginTime="00:00:00"
                                             Duration="00:00:00.500"
                                             Storyboard.TargetName="Seed"
                                             Storyboard.TargetProperty="OpacityMask.Opacity"
                                             From="0"
                                             To="1">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase EasingMode="EaseIn"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation BeginTime="00:00:00"
                                             Duration="00:00:00.250"
                                             Storyboard.TargetName="SeedTranslateTransform"
                                             Storyboard.TargetProperty="X"
                                             From="100"
                                             To="0"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </TabItem.Triggers>
        </TabItem>
    </TabControl>-->
    <controls:MetroWindow.Resources>
        <Style x:Key="StageButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="15"/>
        </Style>
    </controls:MetroWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid x:Name="MainContainerVisualSizeHelper"/>
        <Grid x:Name="MainContainer">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding ElementName=MainContainerVisualSizeHelper,Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=MainContainerVisualSizeHelper,Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=MainContainerVisualSizeHelper,Path=ActualWidth}"/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Grid Grid.Row="1">
            <Button Style="{DynamicResource StageButton}" Content="下一步" Command="{Binding PrevStageCommand}" HorizontalAlignment="Right"/>
            <Button Style="{DynamicResource StageButton}" Content="上一步" Command="{Binding NextStageCommand}" HorizontalAlignment="Left"/>
        </Grid>
    </Grid>
</controls:MetroWindow>
