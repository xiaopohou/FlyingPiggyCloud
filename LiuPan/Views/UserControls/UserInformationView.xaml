<UserControl x:Class="SixCloud.Views.UserControls.UserInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SixCloud.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="110" d:DesignWidth="160">
    <Grid>
        <!--用户头像-->
        <Border CornerRadius="50" BorderThickness="1" Height="50" Margin="0,30,0,0" VerticalAlignment="Top" Width="50">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="#FF5A5A5A"/>
            </Border.Effect>
            <Border.Background>
                <ImageBrush ImageSource="{Binding Icon}" Stretch="UniformToFill"/>
            </Border.Background>
        </Border>
        <ProgressBar ToolTip="{Binding FrendlySpaceCapacity}" Value="{Binding Progress}" Margin="0,30,0,0" VerticalAlignment="Top" Width="50" Height="50" Style="{StaticResource MaterialDesignCircularProgressBar}"/>
        <!--用户名-->
        <TextBlock x:Name="textBlock" Text="{Binding Name,Mode=OneTime}" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,90,0,0" Foreground="#FF5A5A5A"/>
    </Grid>
</UserControl>
